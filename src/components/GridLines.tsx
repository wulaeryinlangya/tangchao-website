/**
 * Full-bleed instrument-console grid: 25% grid lines + plus-mark intersections.
 * Decorative background for the dark data sections (Stats / Data).
 */
export default function GridLines() {
  return <div className="console-grid" aria-hidden="true" />
}
